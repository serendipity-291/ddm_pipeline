[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: fast unit-level checks
    integration: cross-module and API contract checks
    e2e: end-to-end smoke checks (nightly/manual)
    model_smoke: lightweight model behavior smoke checks
addopts = -ra
