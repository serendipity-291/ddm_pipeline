# Create: conda env create -f environment.yml
# Activate: conda activate ddm_pipeline
# Remove:   conda env remove -n ddm_pipeline
name: ddm_pipeline
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - -r requirements.txt
